@version: 3.35
@include "scl.conf"

source s_network {
  network(
    transport("udp")
    port(514)
  );
};

destination d_logstash {
  network(
    "logstash"
    transport("udp")
    port(5140)
  );
};

log {
  source(s_network);
  destination(d_logstash);
};