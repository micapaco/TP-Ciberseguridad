apiVersion: 1

providers:
  - name: 'SIEM Dashboards'
    orgId: 1
    folder: 'SIEM'
    folderUid: 'siem-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
